[project]
name = "ensemble-shared"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = [
    "jsonschema>=4.0",
    "PyYAML>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ensemble_shared"]
