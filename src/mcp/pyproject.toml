[project]
name = "ensemble-mcp"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0",
    "ensemble-backend",
    "ensemble-shared",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ensemble_mcp"]
