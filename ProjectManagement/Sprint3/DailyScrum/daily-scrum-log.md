# Daily Scrum Log — Sprint 3 (20 Temmuz – 2 Ağustos 2026)

> **Platform:** **Slack** (danışman dahil grup) + ekip-içi **WhatsApp** — ikisi birden, **yazılı async, sabit saat yok**. Görsel kanıtlar bu klasörde (`daily-*.png`) ve `../Meetings/` klasöründedir.
> AI bu tabloyu mesaj/toplantı/PR kayıtlarından taslakladı; SM tarafından doğrulanmıştır (D-14).

| Tarih | Öne çıkanlar (dün/bugün) | Blocker/Karar | Kanıt |
|---|---|---|---|
| 2026-07-20 | Sprint 3 (son sprint) başladı — Fatih görev atamalarını yaptı + PR #193'ü (Sprint 3 bağımlılık haritası + kontratlar) açtı; Esma review edip (dogfood iddiasını bot'un ürettiği grafla kendi çalıştırmasını kıyaslayarak doğruladı — 22 kenar birebir) approve etti, Fatih merge etti. Fatih: "bağımlılıklar neredeyse yok, herkes kendi görevinde beklemeden ilerleyebilir — tabii review'lar hariç, onu da paslaşırız." Bootcamp'in son soru-cevap etkinliği akşam 20:00'de duyuruldu. Semih Windows'ta Smart App Control'ün uv'nin Python'ını engelleme sorununu çözüp (imzalı Python 3.12.10'a geçti) kendine atanan radar işlerini bitirdi: #162 (eval↔radar pipeline paritesi, PR #201), #163 (sim=None ayrımı, PR #203), #164 (aynı-yazar düşük-severity uyarısı, PR #205) — üçü de CI yeşil, eval P=1.0/F0.5=0.8929, aynı-yazar gerçek çakışma uyarı kapsamı 0/2→2/2. Ardından Esma'nın 7 PR'ını detaylıca review etti; #195'te (health) geçersiz PEM'le bile "ok" dönmesini, #196'da (graph) override'sız isteğin 500 vermesini, #200'de (onboarding) drafter hatasında yarım kalan `.harness/`'i bizzat repro edip, #202'de (webhook) OpenAPI sözleşme eksikliğini, #204'te (get_diff) `_diff_cache`'in pollar arası bayatladığını işaretleyip değişiklik istedi. Esma paralelde: #193/#198/#199/#201/#203'ü review etti (Dockerfile'ı gerçekten build edip çalıştırdı, `docker-compose.yml`'deki `DATABASE_URL` şema uyuşmazlığını bulup takip issue #206 açtı); kendine atanan 7 issue'nun (#186/#53/#104/#32/#57/#62/#152) hepsine PR açtı (#194/#195/#196/#197/#200/#202/#204); Semih+Fatih'in bulgularının hepsini aynı gün düzeltti — #53 yanıltıcı `ok`/`degraded` → dürüst `configured`/`missing`, #104 `is_active_declared` namespace bug'ı (Fatih'in HIGH bulgusu, kenar-modülü ile presence-modülü farklı namespace'teydi), #57 yarım `.harness/` (Fatih+Semih bağımsız aynı repro — riskli/ağ-çağrılı adım en başa alınıp hata-temizliği eklendi), #62 OpenAPI 400/401 gövdesiz + ASCII-dışı imza `TypeError`'ı, #152 `_diff_cache` pollar-arası bayatlık (aynı sınıf, önceden var olan `_compare_cache` sorunu için ayrı takip issue #207 açıldı). Ayrıca PR'larının hiçbirinde `Closes #<id>` olmadığını fark edip düzeltti (mekanik hataydı, issue'lar merge'de otomatik kapanmıyordu). Fatih akşam bir merge turu yaptı: 6 PR main'e indi (#194 PEM-env · #197 MCP · #200 onboarding · #201 eval-eşik · #203 sim-none · #205 aynı-yazar-low), main 259 test + eval gate yeşil; kalan review-bloklarını dağıttı — Esma'ya #202/#204/#196 (somut reçetelerle), Semih'e #200'ün artık kapatılabilir olduğunu + kendi deploy zincirini bekleten #198/#199'un review'unu, #195 kararını (issue başlığını `configured`/`missing`'e göre netleştirip re-review mi, dismiss mi) ortak bıraktı. Enes Sprint 3 Gün 1 kritik işlerini bitirdi: #179 (psycopg sürücüsü — Postgres deploy blocker'ı açıldı), #52 (`GET /events` + `/presence`, ETag/304 destekli), #48 (`make rebuild` artık GitHub backfill de çekiyor) — 253 test yeşil, PR #208'i tüm ekibi reviewer ekleyerek açtı. | Gün sonunda açık kalanlar: #195/#196/#202/#204'te Esma'nın düzeltmeleri push edildi (CI yeşil) ama Semih/Fatih'in re-review'u bekleniyor; Enes'in #208'i ve Semih'in yeni açtığı #209 (T-31 scope-drift dedektörü) henüz kimse tarafından review edilmedi | `daily-2026-07-20-whatsapp.png` |
